# Core skills

This open-source shell ships two infrastructure skills the app's own features use
(`large-file`, `export-qdpx`). The research **methodology** skills — annotation,
qualitative coding, regression adjudication, journal formatting, methodology review —
are a separate proprietary layer, included in the signed release but not in this
repository. See the README's "Open-core" section.
