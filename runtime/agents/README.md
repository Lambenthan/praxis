# agents

The Praxis agents and guardrail plugins are part of the proprietary methodology
layer — shipped in the signed release, not in this open-source shell. This
placeholder keeps the build's resource path valid.
